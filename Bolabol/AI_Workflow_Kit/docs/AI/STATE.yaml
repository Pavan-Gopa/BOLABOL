# Bolabol 1.0.4 ASR Core ML — live orchestration state
# Orchestrator owns this file.

current_step: S9
step_description: >-
  Engines: CanaryCoreMLEngine (Flash + 1B Path B) + GigaAMCoreMLEngine wired to
  TranscriptionEngineStore, replacing UnavailableTranscriptionEngine stubs.
  Spike constraints enforced (compute units, native frontends, true lengths,
  chunk caps, state resets, explicit language via capabilities). Gate: offline
  dictate produces text. No UI cards (S10), no HUD matrix (S11).
target_files:
  - Sources/NativeBolabol/Stores/TranscriptionEngineStore.swift
  - Sources/NativeBolabol/Engines/
  - Sources/NativeBolabolCore/
  - Tests/NativeBolabolCoreTests/
  - script/qa/check_no_canary_product.sh
  - AI_Workflow_Kit/docs/AI/FEEDBACK.md
completed_steps:
  - S0
  - S1
  - RENAME
  - S1b
  - S1c
  - S2
  - S3
  - S4
  - S5
  - S6
  - S4b
  - S7
  - S8
skipped_steps: []
implementation:
  status: pending
  attempts: 0
review:
  status: pending
  last_feedback_file: AI_Workflow_Kit/docs/AI/FEEDBACK.md
  verdict: null
  note: ''
qa:
  status: pending
  last_report: AI_Workflow_Kit/docs/AI/REPORT.md
  suite: S9
  bugs_open: 0
  note: ''
security:
  next_run: recommended_after_S9
  note: >-
    S8 introduced first real CDN/HF model-download path (SECURITY.md trigger:
    "first CDN download path"). Recommend Security campaign after S9 engines wire
    the download path, or at pre-release — Human decides.
next_actor: coder
track: PRODUCT
prior_track: SPIKE_COMPLETE
go_list:
  - id: canary-180m-flash-coreml
    adr: ADR-014
    source: aufklarer/Canary-180M-Flash-CoreML
  - id: gigaam-v3-rnnt-coreml
    adr: ADR-015
    source: huggingfinger0/gigaam-v3-coreml
  - id: canary-1b-v2-coreml
    adr: ADR-017
    source: bolabol-canary-1b-v2-coreml-r1 Path B (Bolabol CDN package)
excluded:
  - FluidInference/canary-1b-v2-coreml
  - alexwengg/canary-1b-v2-coreml
working_roots:
  - Bolabol
checkpoint:
  last_pre_tag: bolabol/pre-S9
  last_post_tag: bolabol/S8-done
  last_commit: 3659a72
plan_files:
  - BOLABOL_ASR_COREML_INTEGRATION_PLAN.md
  - AI_Workflow_Kit/docs/ASR_COREML_STEPS.md
  - AI_Workflow_Kit/docs/DECISIONS.md
coder_brief: |
  Implement S9 only (step card in ASR_COREML_STEPS.md). Real Core ML engines for
  the ADR-018 GO backends, wired into TranscriptionEngineStore (remove
  UnavailableTranscriptionEngine stubs): CanaryCoreMLEngine (Flash + 1B Path B)
  and GigaAMCoreMLEngine. Port frontend/decode logic from the verified spike
  harnesses: CanaryFlashSpike.swift (Flash), CanarySmdesaiSpike.swift (1B Path
  B), GigaAMCoreMLSpike.swift (GigaAM). Enforce spike constraints: Flash
  .cpuAndNeuralEngine only + NeMo mel frontend + true lengths + <=10s chunks;
  1B macOS 15+ MLState gate + Path B frontend constants + <=15s chunks + fresh
  MLState per segment + native SentencePiece from canary_spe.model; GigaAM HTK
  log-mel + 16kHz mono + <=30s chunks + RNNT state reset per chunk + blank 1024.
  Explicit language only (no auto-detect); consume capabilities, NOT
  languageSupport (NB-4). Load from S8 storage roots, require S8 complete-folder
  presence. Honest errors (missing model / unsupported OS). Narrow
  check_no_canary_product.sh per ADR-018 (engines allowed from S9; still forbid
  NO-GO HF sources + Python). No S10 UI cards/banners, no S11 HUD matrix.
notes: |
  S8 cycle closed with bug loop: POST bolabol/S8-done @ 3659a72 (LOCAL ONLY —
  monorepo remote push DISABLED; Human must push when enabled). Graphify rebuilt
  after POST (4723 nodes). S9 step card written. next_actor: coder for S9.
  NB-4 must be honored in S9 (capabilities, not languageSupport). Security
  campaign recommended after S9 (first CDN download path + engines now live).
