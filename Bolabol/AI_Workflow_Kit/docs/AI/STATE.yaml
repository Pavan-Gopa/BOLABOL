# Bolabol 1.0.4 ASR Core ML — live orchestration state
# Orchestrator owns this file.

current_step: S9
step_description: >-
  Engines: CanaryCoreMLEngine (Flash + 1B Path B) + GigaAMCoreMLEngine wired to
  TranscriptionEngineStore, replacing UnavailableTranscriptionEngine stubs.
  Spike constraints enforced (compute units, native frontends, true lengths,
  chunk caps, state resets, explicit language via capabilities). Gate: offline
  dictate produces text. No UI cards (S10), no HUD matrix (S11).
target_files:
  - Sources/NativeBolabol/Engines/CanaryCoreMLEngine.swift
  - Tests/NativeBolabolCoreTests/S9RuntimeSmokeTests.swift
  - AI_Workflow_Kit/docs/AI/FEEDBACK.md
completed_steps:
  - S0
  - S1
  - RENAME
  - S1b
  - S1c
  - S2
  - S3
  - S4
  - S5
  - S6
  - S4b
  - S7
  - S8
  - S9
skipped_steps: []
implementation:
  status: done
  attempts: 2
review:
  status: approved
  last_feedback_file: AI_Workflow_Kit/docs/AI/FEEDBACK.md
  verdict: approved
  note: >-
    Independent Reviewer approved the BUG-003 fix on 2026-08-05. The review
    verified the real product decoder seam, rank-1 int32 pos regression test,
    full gate, and all three opt-in real runtime smokes. Tester must now perform
    independent feature QA and confirm BUG-003 closure.
qa:
  status: green
  last_report: AI_Workflow_Kit/docs/AI/REPORT.md
  suite: S9
  bugs_open: 0
  note: >-
    Independent Tester QA rerun is green: the product rank regression, full
    suite (555 tests), full QA gate (29/29), dedicated real Path B smoke, and
    all three S9 runtime smokes passed. BUG-003 is CLOSED; no open S9 product
    defects remain.
security:
  next_run: deferred_human_testing
  note: >-
    Human decision 2026-08-04: Security campaign deferred — Human will manually
    test the app first; Security kick after that (Human + Orchestrator decide
    scope). Trigger context: S8 first CDN download path + S9 live engines.
next_actor: architect
track: PRODUCT
prior_track: SPIKE_COMPLETE
go_list:
  - id: canary-180m-flash-coreml
    adr: ADR-014
    source: aufklarer/Canary-180M-Flash-CoreML
  - id: gigaam-v3-rnnt-coreml
    adr: ADR-015
    source: huggingfinger0/gigaam-v3-coreml
  - id: canary-1b-v2-coreml
    adr: ADR-017
    source: bolabol-canary-1b-v2-coreml-r1 Path B (Bolabol CDN package)
excluded:
  - FluidInference/canary-1b-v2-coreml
  - alexwengg/canary-1b-v2-coreml
working_roots:
  - Bolabol
checkpoint:
  last_pre_tag: bolabol/pre-S9
  last_post_tag: bolabol/S9-done
  last_commit: 2cfbc72
plan_files:
  - BOLABOL_ASR_COREML_INTEGRATION_PLAN.md
  - AI_Workflow_Kit/docs/ASR_COREML_STEPS.md
  - AI_Workflow_Kit/docs/DECISIONS.md
coder_brief: |
  Fix BUG-003 only. The real Canary 1B Path B runtime smoke loads the GO package
  but fails before decoding because product code creates pos with rank 2 ([1, 1])
  while the Core ML decoder requires rank 1. Reconcile the product input shape
  with the verified S4b Path B harness/model contract; preserve all strict S9
  constraints and existing Flash/GigaAM behavior. Add or adjust only minimum
  non-fake regression coverage needed to prove the product sends the required
  shape, then run the opt-in real smoke when documented scratch assets exist.
  Do not change S10+ UI/HUD work, catalog/download behavior, Tester findings,
  BUG_REPORT.md, or workflow state.
notes: |
  S8 cycle closed: POST bolabol/S8-done @ 3659a72 (LOCAL ONLY — monorepo remote
  push DISABLED; Human must push when enabled). PRE bolabol/pre-S9 @ ca10a95.
  S9 loop: Coder handoff → Reviewer CHANGES_REQUESTED (BLOCK-S9-001..004) →
  Coder Fix Attempt 1 (all 4 blocking closed, 552 tests, 28/28 QA) → Graphify
  rebuilt (4942 nodes) → Reviewer re-review approved. Tester then completed
  feature QA: 554 tests and 29/29 QA were green, but real Canary 1B Path B
  smoke was red with BUG-003. Coder fixed only BUG-003: product pos is now
  rank 1, targeted/full tests, 29/29 QA, all three real runtime smokes, and a
   fresh build are reported green. Graphify was rebuilt (4,988 nodes) and the
   independent Reviewer approved BUG-003. Independent Tester QA then passed:
   555 tests, 29/29 QA checks, the real Path B smoke, and all three runtime
   smokes; BUG-003 is closed and S9 is complete. Graphify POST rebuild is
   required. S10 is named in the authoritative ASR plan but lacks a detailed
   executable card, so Architect must first produce a design-only scope packet.
   Security remains deferred until Human manual testing and a green S9 feature
   gate.
