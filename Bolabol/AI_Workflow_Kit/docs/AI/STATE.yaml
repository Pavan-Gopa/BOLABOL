# Bolabol 1.0.4 ASR Core ML — live orchestration state
# Orchestrator owns this file.

current_step: VERTICAL-PULSE-HUD
step_description: >-
  Polish the third vertical HUD style, Vertical Pulse. Fit the complete D/1/2/3/4
  prompt row without clipping, keep the main capsule at a pixel-stable screen
  anchor while R/1/2 and accessory controls change, and add a right-click language
  menu on the left language control using all configured settings languages. Restore
  explicit Canary source switching between supported Primary and Additional languages;
  Canary 1B with ru/en must switch R/E instead of being fixed to R. Translation-target
  selection must still respect ADR-022: no Canary/GigaAM speech translation.
target_files:
  - AI_Workflow_Kit/docs/AI/FEEDBACK.md
  - Sources/NativeBolabol/Services/HotkeySessionOverlayManager.swift
  - Sources/NativeBolabol/Stores/TranscriptionEngineStore.swift
  - Sources/NativeBolabol/Views/ContentView.swift
  - Sources/NativeBolabol/Views/HUDLanguagePickerPopoverView.swift
  - Sources/NativeBolabolCore/Models/GeneralSettings.swift
  - Sources/NativeBolabolCore/Models/UserSpeechLanguages.swift
  - Sources/NativeBolabolCore/Services/AppText.swift
  - Sources/NativeBolabolCore/Services/HUDQuickSwitcherLayout.swift
  - Tests/NativeBolabolCoreTests/ApplicationWideRegressionContractTests.swift
  - Tests/NativeBolabolCoreTests/HUDLayoutAndComposerTests.swift
  - Tests/NativeBolabolCoreTests/HUDLanguagePickerPopoverTests.swift
  - Tests/NativeBolabolCoreTests/HUDProviderSwitcherFeatureTests.swift
  - Tests/NativeBolabolCoreTests/NewFeatureRegressionTests.swift
  - Tests/NativeBolabolCoreTests/S11SessionRoutingTests.swift
  - Tests/NativeBolabolCoreTests/SettingsLocalizationTests.swift
  - Tests/NativeBolabolCoreTests/UserSpeechLanguagesTests.swift
  - Tests/NativeBolabolCoreTests/TranscriptionLanguageRoutingTests.swift
  - script/qa/check_vertical_pulse_hud_contract.sh
completed_steps:
  - S0
  - S1
  - RENAME
  - S1b
  - S1c
  - S2
  - S3
  - S4
  - S5
  - S6
  - S4b
  - S7
  - S8
  - S9
skipped_steps: []
implementation:
  status: changes_requested
  attempts: 5
review:
  status: changes_requested
  last_feedback_file: AI_Workflow_Kit/docs/AI/FEEDBACK.md
  verdict: changes_requested_after_coder_fix_attempt_3
  note: >-
    Independent Reviewer rejected Coder Fix Attempt 3. HR-VPH-002 catalog/UI
    restoration is accepted, but HR-VPH-001 remains open because the shared hit
    frame does not match the real Vertical Pulse visible control geometry and
    the 8-10 pt margin is not part of the SwiftUI Button left-click hit area.
    Additional blockers: arbitrary picker selection still mutates persisted
    Primary/Additional settings, popover lifecycle/session/display invalidation
    remains incomplete, Canary target-purpose menu policy is not fail-closed
    against ADR-022-invalid target selection, and tests/guard still miss those
    production surfaces.
qa:
  status: blocked
  last_report: AI_Workflow_Kit/docs/AI/REPORT.md
  suite: FINAL-APPLICATION-EXHAUSTIVE
  bugs_open: 2
  note: >-
    Blocked until independent Reviewer approval. Then final Tester must inventory
    and cover the whole application: every window, menu, button, left/right click,
    keyboard/hotkey, hover, scroll, drag, modal, settings control, persistence,
    localization, accessibility, error/retry and user-visible workflow. Rerun all
    BUG-HHP/ADR-022/HUD coverage and add reliable parameterized runners totaling
    at least 2,000-3,000 scenario/assertion cases, not thousands of brittle scripts.
security:
  next_run: after_final_exhaustive_qa
  note: >-
    Run a dedicated full Security campaign after Reviewer approval and final
    exhaustive Tester QA are green. Security remains a separate fresh agent and
    must audit the full application attack surface without editing product code.
next_actor: coder
track: PRODUCT
prior_track: SPIKE_COMPLETE
go_list:
  - id: canary-180m-flash-coreml
    adr: ADR-014
    source: aufklarer/Canary-180M-Flash-CoreML
  - id: gigaam-v3-rnnt-coreml
    adr: ADR-015
    source: huggingfinger0/gigaam-v3-coreml
  - id: canary-1b-v2-coreml
    adr: ADR-017
    source: bolabol-canary-1b-v2-coreml-r1 Path B (Bolabol CDN package)
excluded:
  - FluidInference/canary-1b-v2-coreml
  - alexwengg/canary-1b-v2-coreml
working_roots:
  - Bolabol
checkpoint:
  last_pre_tag: bolabol/pre-VERTICAL-PULSE-HUD
  last_post_tag: bolabol/S9-done
  last_commit: 4d84ff2
plan_files:
  - BOLABOL_ASR_COREML_INTEGRATION_PLAN.md
  - AI_Workflow_Kit/docs/ASR_COREML_STEPS.md
  - AI_Workflow_Kit/docs/DECISIONS.md
coder_brief: |
  Fix Attempt 3 is a narrow Human-directed restoration. Keep the current popover
  component, search, scrolling and internal safety fixes, but restore its compact
  approved width: approximately 30% narrower than the current 280 pt maximum, with
  no large empty area. For explicit Canary, show all verified model source languages.
  For Auto/Whisper/Cloud target selection, show Auto plus the complete existing
  25-European-language target catalog, never only Primary/Additional. Keep selected,
  search and scroll behavior. Rework A/E hit geometry so 100% of the visible circle
  plus a forgiving 8-10 pt margin is clickable at every scale, without overlapping
  target/spectrum. Do not redesign anything else. Add regression tests for exact
  catalog count/order, compact width contract and full-circle hit coverage.
notes: |
  Orchestrator status build after Fix Attempt 3 review: old Bolabol process was
  closed, `swift package clean && ./script/build_and_run.sh --verify` succeeded,
  and fresh signed Release `dist/Bolabol.app` is running as PID 36211. Orchestrator
  did not run tests/QA. Signature verification returned success with no output.

  VERTICAL-PULSE-HUD Fix Attempt 3 independent review returned CHANGES_REQUESTED.
  Coder Fix Attempt 4 must be narrow: fix real left-click hit geometry/margin by
  aligning AppKit policy with actual SwiftUI button geometry or expanding the
  real Button content shape; prevent any arbitrary picker selection from mutating
  persisted Primary/Additional settings; close/invalidate popovers on stop, hide,
  processing, outside close, display changes and stale callbacks; reject Canary
  targetLanguageSelection policy requests as ADR-022 invalid; and add tests/guard
  mutations that exercise these production surfaces. Preserve the Human-approved
  compact picker and Auto+25 / Canary source catalogs, which Reviewer accepted.

  VERTICAL-PULSE-HUD Fix Attempt 3 handoff is ready for independent review.
  Orchestrator synced STATE to waiting_review and rebuilt GraphiFy before Reviewer:
  6,401 nodes / 14,374 edges. Human reports the fresh app now works as desired,
  but Human acceptance does not replace independent review, final exhaustive QA,
  or the scheduled Security campaign after QA green.

  S8 cycle closed: POST bolabol/S8-done @ 3659a72 (LOCAL ONLY — monorepo remote
  push DISABLED; Human must push when enabled). PRE bolabol/pre-S9 @ ca10a95.
  S9 loop: Coder handoff → Reviewer CHANGES_REQUESTED (BLOCK-S9-001..004) →
  Coder Fix Attempt 1 (all 4 blocking closed, 552 tests, 28/28 QA) → Graphify
  rebuilt (4942 nodes) → Reviewer re-review approved. Tester then completed
  feature QA: 554 tests and 29/29 QA were green, but real Canary 1B Path B
  smoke was red with BUG-003. Coder fixed only BUG-003: product pos is now
  rank 1, targeted/full tests, 29/29 QA, all three real runtime smokes, and a
   fresh build are reported green. Graphify was rebuilt (4,988 nodes) and the
   independent Reviewer approved BUG-003. Independent Tester QA then passed:
   555 tests, 29/29 QA checks, the real Path B smoke, and all three runtime
   smokes; BUG-003 is closed and S9 is complete at POST bolabol/S9-done @
   2cfbc72 (LOCAL ONLY — remote push DISABLED). Graphify was rebuilt after POST.
   Architect handoff appended ADR-019 (S10 capability/UI/banner contract) with
   no product changes. S10 begins from PRE bolabol/pre-S10. Coder implementation
   handoff reports focused suites, full swift test (563), real S9 smoke, and
   build green; run_all is 27/29 because legacy S1b/S6 scope allowlists flag
   accepted S10 paths. Graphify rebuilt (5072 nodes) before independent review.
   Reviewer returned changes_requested: BLOCK-S10-001 must restore real S8
   install controls/progress under Canary hard-language block while preventing
   auto-activation; focused coverage is missing. Coder Fix Attempt 1 reports
   565 tests, real S9 smoke, and build green; Reviewer re-review approved the
   fix. Legacy S1b/S6 allowlist failures are a proven static QA gap now queued
   for minimal Tester remediation. User-confirmed current dist/Bolabol.app v1.0.3 runtime
   reproduction on 2026-08-05 confirmed Canary Flash/GigaAM route with
   forcedLanguageCode=none and resolvedLanguageCode=auto (Whisper only), then
   empty text. Flash/GigaAM package layouts in AI_LOCAL_MODELS are complete;
   Canary 1B is not downloaded because its CDN hostname resolution failed and
   its local folder is empty. Tester is paused; Architect must resolve exact
   S11 runtime-routing/download mitigation before Coder is dispatched. Security
   remains deferred until Human manual testing and a green S9 feature gate.
   Architect completed a design-only investigation and appended proposed ADR-020,
   confirming the runtime root cause: active engine selection is correct, but
   legacy Whisper-oriented routing converts auto to nil before Canary/GigaAM
   requests; Sidebar and audio-modal re-transcription duplicate the defect. ADR-020
   requires an immutable explicit-source session plan across every entry point,
   preserves Whisper/Parakeet auto, and treats Canary 1B -1003 as a separate
   truthful infrastructure/download failure with no guessed endpoint or fallback.
    Combined S10/S11 QA stays blocked through implementation, independent review,
    CDN disposition, and Tester fresh-build manual matrix. Graphify rebuilt after
    Architect handoff: 5,573 nodes / 12,462 edges. ADR-022 ASR-only cleanup was
    independently approved and captured at checkpoint commit 4d84ff2 before the
    Human-requested Vertical Pulse HUD cycle. PRE bolabol/pre-VERTICAL-PULSE-HUD
    is local only because remote push remains disabled.
