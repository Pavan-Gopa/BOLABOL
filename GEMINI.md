# GEMINI.md

This is a multi-project workspace root. It is not the SmartScribe, VaniScript, or MindOcean app root.

Run project-specific commands from the relevant project folder, not from `AI Projects/`.

Use `scripts/check-workspace-root-clean.sh` to detect accidental app files placed at the workspace root.

